"use client";

export default function CalculadoraPVPPage() {
  return (
    <div>
      <h1 style={{ fontSize: 34, marginBottom: 8 }}>🧮 Calculadora PVP</h1>
      <p style={{ color: "#aaa" }}>
        Aquí irá: seleccionar variante → proveedores ordenados → IVA 0/15 → calcular sin/ con IVA → guardar precio venta.
      </p>

      <div style={{ marginTop: 18, padding: 16, border: "1px solid #222", background: "#111", borderRadius: 14 }}>
        <b>En el siguiente paso:</b> construiremos esta calculadora completa.
      </div>
    </div>
  );
}